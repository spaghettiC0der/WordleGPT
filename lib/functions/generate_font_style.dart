import 'package:flutter/material.dart';

TextStyle generateStyle(FontWeight weight, Color color, double size) {
  return TextStyle(
    fontFamily: 'Montserrat',
    fontWeight: weight,
    color: color,
    fontSize: size,
  );
}
